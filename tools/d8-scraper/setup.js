const fs=require('fs'),path=require('path'),readline=require('readline');
const rl=readline.createInterface({input:process.stdin,output:process.stdout});
const ask=q=>new Promise(r=>rl.question(q,a=>r(a.trim())));
(async()=>{console.log('\nНастройка на Digital Eight Scraper\n');const dashboardUrl=await ask('Dashboard URL [https://finance.management.digitaleight.bg]: ')||'https://finance.management.digitaleight.bg';let username='';while(!username)username=await ask('Потребител (Admin/Partner) - задължително: ');let password='';while(!password)password=await ask('Парола - задължително: ');process.stdout.write('\nПроверявам входа... ');const r=await fetch(dashboardUrl.replace(/\/$/,'')+'/api.php?action=login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.authToken)throw Error(d.error||'Dashboard входът е неуспешен');fs.writeFileSync(path.join(__dirname,'config.local.json'),JSON.stringify({dashboardUrl,username,password},null,2));rl.close();console.log('OK\nНастройките са валидни и са запазени само на този компютър.\n')})().catch(e=>{rl.close();console.error('\nГРЕШКА: '+e.message+'\nНастройките НЕ са запазени. Пусни SETUP SCRAPER.cmd отново.\n');process.exit(1)});